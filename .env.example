# Ollama Configuration
# Base URL for Ollama API
OLLAMA_BASE_URL=http://localhost:11434

# Default Ollama model to use
OLLAMA_MODEL=llama2

# Server Configuration
# Port for the backend server
PORT=8000

# Database Configuration (Optional)
# Uncomment and configure if using PostgreSQL instead of SQLite
# DATABASE_URL=postgresql://username:password@localhost:5432/docuchat

# Frontend Configuration
# These are automatically set by Vite, no need to change
# VITE_API_URL is handled by the proxy in vite.config.js
